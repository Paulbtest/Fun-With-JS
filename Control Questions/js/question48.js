let userNumber = Number(prompt("Введите число: "));

let calculateSquare = function(number) {
    return number ** 2;
};

console.log(calculateSquare(userNumber));

//Сделайте функцию, которая возвращает квадрат числа. Число передается параметром.